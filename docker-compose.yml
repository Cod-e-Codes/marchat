services:
  server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - MARCHAT_PORT=8080
      - MARCHAT_DB_PATH=/data/marchat.db
    volumes:
      - marchat-data:/data

volumes:
  marchat-data:
